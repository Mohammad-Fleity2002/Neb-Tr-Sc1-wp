version: 0.0
os: linux
files:
  - source: /
    destination: /var/www/html
    overwrite: yes
hooks:
  AfterInstall:
    - location: scripts/fix_permissions.sh
      timeout: 60
      runas: root
  ApplicationStart:
    - location: scripts/restart_services.sh
      timeout: 60
      runas: root

